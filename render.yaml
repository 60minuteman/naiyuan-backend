services:
  - type: web
    name: naiyuan-backend
    env: node
    buildCommand: ./build.sh
    startCommand: yarn start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: naiyuan-db
          property: connectionString

